enum RoleEnum {
    ADMIN = 'ADMIN',
    USER = 'USER'
}

const RoleList = [
    { name: 'admin', value: RoleEnum.ADMIN },
    { name: 'admin', value: RoleEnum.USER },
];

export { RoleEnum, RoleList };
